import { Button } from "@/components/ui/button";
import React from "react";

const AddToCartButton = () => {
  return <Button className="mt-auto">Agregar al carrito</Button>;
};

export default AddToCartButton;
